- Moving every DMR packet decoding related stuff out of ipsc.c
- Relying only on the decoded DMR packet data when processing call starts and ends
- Idle message handling
- CSBK and Multi Block Control (MBC) message decoding
- Handling other DPF data types
- Inserting decoded SMS messages into a database table
- GPS position data decoding
- SMS sending
- Serving raw and decoded voice data using a built-in websocket server
- Voice sending

- CACH decoding including variable length BPTC for CACH signalling.
  As IPSC packets don't contain CACH fields this can only be done using an SDR receiver.
- Reverse channel handling including variable length BPTC for reverse channel decoding
